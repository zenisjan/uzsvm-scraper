# Core dependencies
apify >= 2.3.0, < 3.0.0
apify-shared >= 1.3.0, < 2.0.0
apify-client >= 1.11.0, < 2.0.0
crawlee < 0.6.0
pydantic >= 2.0.0, < 2.11.0
httpx>=0.24.0
beautifulsoup4[lxml]
psycopg2-binary>=2.9.0

# Data processing
python-dateutil>=2.8.0
